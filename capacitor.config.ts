import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.davi.dev.pokemonangular',
  appName: 'project-angular-pokemon',
  webDir: 'dist/project-angular-pokemon/browser'
};

export default config;
